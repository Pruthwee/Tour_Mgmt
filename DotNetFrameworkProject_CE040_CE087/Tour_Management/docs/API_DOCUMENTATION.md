# API Documentation

The web application exposes Razor Pages for Tours, Customers, and Bookings with complete CRUD workflows. A lightweight `/health` endpoint returns application status for monitoring.
