apiVersion: v1
kind: Service
metadata:
  name: tour-management-service
  namespace: tour-management
  labels:
    app: tour-management
    environment: production
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: tour-management
